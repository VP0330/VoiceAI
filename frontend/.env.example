# Frontend API Configuration
# Use VITE_ prefix for variables to be exposed to the client

# API Base URL (for fetch requests)
# Leave empty to use relative paths (recommended for development with Vite proxy)
VITE_API_BASE_URL=

# WebSocket API Host (host:port format)
# Leave empty to use localhost:8000
VITE_API_HOST=localhost:8000
